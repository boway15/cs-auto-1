app:
  description: 跨境电商客服邮件智能分析工作流。提取意图、订单号、情绪、风险等级等关键信息。
  icon: "\U0001F4EC"
  icon_background: '#E0F2FE'
  icon_type: emoji
  mode: workflow
  name: 独立站智能客服-邮件智能分析
  use_icon_as_answer_icon: false
dependencies:
  - current_identifier: null
    type: marketplace
    value:
      marketplace_plugin_unique_identifier: langgenius/deepseek:0.0.15@725407927b04e236212083d20e92830d60fa944e42cd357ef6902c160414f6f1
      version: null
kind: app
version: 0.6.0
workflow:
  conversation_variables: []
  environment_variables: []
  features:
    file_upload:
      allowed_file_extensions:
      - .JPG
      - .JPEG
      - .PNG
      - .GIF
      - .WEBP
      - .SVG
      allowed_file_types:
      - image
      - document
      allowed_file_upload_methods:
      - local_file
      - remote_url
      enabled: true
      fileUploadConfig:
        attachment_image_file_size_limit: 2
        audio_file_size_limit: 50
        batch_count_limit: 5
        file_size_limit: 15
        file_upload_limit: 20
        image_file_batch_limit: 10
        image_file_size_limit: 10
        single_chunk_attachment_limit: 10
        video_file_size_limit: 100
        workflow_file_upload_limit: 10
      image:
        enabled: false
        number_limits: 3
        transfer_methods:
        - local_file
        - remote_url
      number_limits: 3
    opening_statement: ''
    retriever_resource:
      enabled: true
    sensitive_word_avoidance:
      enabled: false
    speech_to_text:
      enabled: false
    suggested_questions: []
    suggested_questions_after_answer:
      enabled: false
    text_to_speech:
      enabled: false
      language: ''
      voice: ''
  graph:
    edges:
      - data:
          isInIteration: false
          isInLoop: false
          sourceType: start
          targetType: llm
        id: edge_start_to_llm
        source: start
        sourceHandle: source
        target: llm_analyze
        targetHandle: target
        type: custom
        zIndex: 0
      - data:
          isInIteration: false
          isInLoop: false
          sourceType: llm
          targetType: code
        id: edge_llm_to_code
        source: llm_analyze
        sourceHandle: source
        target: code_parse
        targetHandle: target
        type: custom
        zIndex: 0
      - data:
          isInIteration: false
          isInLoop: false
          sourceType: code
          targetType: end
        id: edge_code_to_end
        source: code_parse
        sourceHandle: source
        target: end
        targetHandle: target
        type: custom
        zIndex: 0
    nodes:
      - data:
          desc: ''
          selected: false
          title: 开始
          type: start
          variables:
            - label: 邮件主题
              max_length: 1000
              options: []
              required: true
              type: text-input
              variable: subject
            - label: 邮件正文
              max_length: 50000
              options: []
              required: true
              type: paragraph
              variable: body_text
            - label: 客户最新回复（无引用）
              max_length: 50000
              options: []
              required: false
              type: paragraph
              variable: body_latest
            - label: 发件人邮箱
              max_length: 500
              options: []
              required: true
              type: text-input
              variable: from_email
            - label: 附件信息
              max_length: 5000
              options: []
              required: false
              type: text-input
              variable: attachments
            - label: 附件内容
              allowed_file_types:
              - image
              - document
              allowed_file_upload_methods:
              - local_file
              - remote_url
              max_length: 10
              required: false
              type: file-list
              variable: attachment_files
        height: 198
        id: start
        position:
          x: 80
          y: 260
        positionAbsolute:
          x: 80
          y: 260
        selected: false
        sourcePosition: right
        targetPosition: left
        type: custom
        width: 244
      - data:
          desc: 使用 LLM 分析邮件意图、情绪、风险等级等信息
          title: AI 邮件分析
          type: llm
          model:
            completion_params:
              temperature: 0.2
            mode: chat
            name: deepseek-v4-flash
            provider: langgenius/deepseek/deepseek
          prompt_template:
            - role: system
              text: |
                You are an email analysis assistant for a cross-border e-commerce customer service system. Analyze the customer email and return a JSON object.

                ## Output Format
                Return ONLY one valid JSON object: no markdown, no code fences, no chain-of-thought or XML-like tags (e.g. <thinking>, <think>), and no characters before the opening "{". Example:
                {"intent":"after_sale","business_intent":"damaged","category":"售后","order_no":"SO20240315001","is_info_complete":false,"missing_elements":["order_no","attachment"],"summary":"客户反馈收到的商品有破损，要求退款","priority":"high","risk_level":"normal","sentiment":"frustrated","language":"zh","entities":{"order_no":null,"product_name":"Wireless Mouse"}}

                ## Fields
                - intent: legacy field. one of "after_sale" (售后), "refund" (退款), "cancel_order" (取消), "change_address" (改地址), "shipping_query" (物流), "general" (其他)
                - business_intent: REQUIRED, exactly one of:
                    "order_cancel"          (订单取消)
                    "address_change"        (订单改地址)
                    "damaged"               (破损)
                    "defect"                (产品缺陷)
                    "description_mismatch"  (商品描述不符)
                    "logistics"             (物流问题)
                    "other"                 (其他问题)
                    "amazon_marketplace"    (亚马逊渠道购买/沟通)
                    "product_inquiry"       (售前/安装咨询)
                    "conversation_idle"     (感谢/结案/无新诉求)
                    "solution_accepted"     (接受客服已提出的方案，待履约)
                - category: "售后" for order_cancel/address_change/damaged/defect/description_mismatch; "咨询" for logistics/other/amazon_marketplace/product_inquiry/conversation_idle/solution_accepted
                - order_no: extract order number if present. Typical shape: one or more Latin letters followed by one or more digits (lengths variable); an optional single - / _ may appear between letters and digits. Also honor labeled lines (订单号 / Order No). Use null if not found.
                - is_info_complete: for damaged/defect/description_mismatch: true only if order_no is present AND the email has at least one attachment; for order_cancel/address_change/logistics: true if order_no is present; otherwise follow rules below
                - missing_elements: array of what's missing. Values: "order_no", "attachment" (no file attached when quality/damage/defect/mismatch context needs evidence; use "attachment" not "image"), "image" (legacy), "product" (unclear product). Empty array if complete.
                - summary: concise Chinese summary of the customer's issue, max 200 characters
                - priority: "urgent" (angry / cancel / address change), "high" (after-sale / refund), "normal" (shipping query / general), "low" (just inquiry)
                - risk_level: "high" if customer wants to cancel order or change address, otherwise "normal"
                - sentiment: customer emotion - "angry", "frustrated", "neutral", "happy"
                - language: "en" (English), "zh" (Chinese), "other" (other languages)
                - entities: object with extracted entities. Include order_no (string or null), product_name (string or null), amount (string or null)

                ## Rules
                1. If customer asks to cancel an order → intent: "cancel_order", business_intent: "order_cancel", risk_level: "high"
                2. If customer asks to change shipping address → intent: "change_address", business_intent: "address_change", risk_level: "high"
                3. If customer reports damage/broken/crushed item → business_intent: "damaged"; if no attachment in email add "attachment" to missing_elements (any attachment counts)
                4. If customer reports product not working / defective / quality issue (non-physical damage) → business_intent: "defect"; if no attachment add "attachment" when evidence is needed
                5. If customer says product does not match description / wrong color / wrong size / "not as described" → business_intent: "description_mismatch"; if no attachment add "attachment" when evidence is needed
                6. Shipping/tracking/delivery/late package questions → intent: "shipping_query", business_intent: "logistics"
                7. For after-sale issues without any order number → add "order_no" to missing_elements; for logistics/cancel/address without order_no in text → add "order_no"
                8. Classify using ONLY the customer's latest reply in "Latest body" below. Quoted thread history must NOT drive business_intent (e.g. do not label "damaged" because an old quote mentions defect).
                9. Thank you / case closed / no new issue in latest reply → business_intent: "conversation_idle"
                10. Customer agrees to a refund/replacement/solution already offered → business_intent: "solution_accepted"
                11. Purchase or inquiry clearly on Amazon / Amazon order id 123-1234567-1234567 → business_intent: "amazon_marketplace"
                12. Pre-sales or installation/how-to questions → business_intent: "product_inquiry"
                13. If none fit → business_intent: "other"
                14. Chinese customers → language "zh"; English → "en"
                15. business_intent must be exactly one value from the list (single-select).
            - role: user
              text: |
                Subject: {{#start.subject#}}
                From: {{#start.from_email#}}
                Latest body (customer's new reply only):
                {{#start.body_latest#}}

                Full body (with quotes, reference only):
                {{#start.body_text#}}

                Attachments: {{#start.attachments#}}
          vision:
            enabled: false
            configs:
              variable_selector: []
          memory:
            enabled: false
            window:
              enabled: false
              size: 50
          context:
            enabled: false
            variable_selector: []
          structured_output:
            enabled: false
          retry_config:
            enabled: false
            max_retries: 1
            retry_interval: 1000
            exponential_backoff:
              enabled: false
              multiplier: 2
              max_interval: 10000
          variables: []
        height: 90
        id: llm_analyze
        position:
          x: 420
          y: 260
        positionAbsolute:
          x: 420
          y: 260
        selected: false
        sourcePosition: right
        targetPosition: left
        type: custom
        width: 244
      - data:
          code: |
            def main(llm_output: str) -> dict:
                import json
                import re

                if llm_output is None:
                    raw = ""
                elif not isinstance(llm_output, str):
                    raw = str(llm_output)
                else:
                    raw = llm_output

                for pat in (
                    r"<think>.*?</think>",
                    r"<thinking>.*?</thinking>",
                ):
                    raw = re.sub(pat, "", raw, flags=re.DOTALL | re.IGNORECASE)
                raw = raw.strip()

                def try_loads(s: str) -> dict:
                    try:
                        x = json.loads(s)
                        return x if isinstance(x, dict) else {}
                    except (json.JSONDecodeError, TypeError):
                        return {}

                result = try_loads(raw)
                if not result:
                    m = re.search(r"```(?:json)?\s*([\s\S]*?)\s*```", raw)
                    if m:
                        result = try_loads(m.group(1).strip())

                if not result:
                    i = raw.find("{")
                    if i >= 0:
                        try:
                            obj, _ = json.JSONDecoder().raw_decode(raw[i:])
                            result = obj if isinstance(obj, dict) else {}
                        except (json.JSONDecodeError, TypeError, ValueError):
                            result = {}

                if not result:
                    m = re.search(r"\{[\s\S]*\}", raw)
                    if m:
                        result = try_loads(m.group(0))

                if not isinstance(result, dict):
                    result = {}

                # Default values for missing fields
                defaults = {
                    "intent": "general",
                    "business_intent": "other",
                    "category": "咨询",
                    "order_no": None,
                    "is_info_complete": False,
                    "missing_elements": [],
                    "summary": "AI 分析失败，请人工处理",
                    "priority": "normal",
                    "risk_level": "normal",
                    "sentiment": "neutral",
                    "language": "en",
                    "entities": {},
                }
                for k, v in defaults.items():
                    if k not in result or result[k] is None:
                        result[k] = v

                # Validate intent
                valid_intents = ["after_sale", "refund", "cancel_order", "change_address", "shipping_query", "general"]
                if result["intent"] not in valid_intents:
                    result["intent"] = "general"

                # Validate business_intent (10 categories, single-select)
                valid_business_intents = [
                    "order_cancel", "address_change", "damaged", "defect",
                    "description_mismatch", "logistics", "other",
                    "amazon_marketplace", "product_inquiry",
                    "conversation_idle", "solution_accepted",
                ]
                if result["business_intent"] not in valid_business_intents:
                    fallback = {
                        "cancel_order": "order_cancel",
                        "change_address": "address_change",
                        "shipping_query": "logistics",
                    }.get(result["intent"], "other")
                    result["business_intent"] = fallback

                # Validate priority
                valid_priorities = ["low", "normal", "high", "urgent"]
                if result["priority"] not in valid_priorities:
                    result["priority"] = "normal"

                return result
          code_language: python3
          desc: 解析 LLM 返回的 JSON，补充默认值，确保结构完整
          outputs:
            intent:
              children: null
              type: string
            business_intent:
              children: null
              type: string
            category:
              children: null
              type: string
            order_no:
              children: null
              type: string
            is_info_complete:
              children: null
              type: boolean
            missing_elements:
              children: null
              type: array[string]
            summary:
              children: null
              type: string
            priority:
              children: null
              type: string
            risk_level:
              children: null
              type: string
            sentiment:
              children: null
              type: string
            language:
              children: null
              type: string
            entities:
              children: null
              type: object
          selected: false
          title: 解析与校验
          type: code
          variables:
            - value_selector:
                - llm_analyze
                - text
              variable: llm_output
        height: 90
        id: code_parse
        position:
          x: 760
          y: 260
        positionAbsolute:
          x: 760
          y: 260
        selected: false
        sourcePosition: right
        targetPosition: left
        type: custom
        width: 244
      - data:
          desc: ''
          outputs:
            - value_selector:
                - code_parse
                - intent
              value_type: string
              variable: intent
            - value_selector:
                - code_parse
                - business_intent
              value_type: string
              variable: business_intent
            - value_selector:
                - code_parse
                - category
              value_type: string
              variable: category
            - value_selector:
                - code_parse
                - order_no
              value_type: string
              variable: order_no
            - value_selector:
                - code_parse
                - is_info_complete
              value_type: boolean
              variable: is_info_complete
            - value_selector:
                - code_parse
                - missing_elements
              value_type: array[string]
              variable: missing_elements
            - value_selector:
                - code_parse
                - summary
              value_type: string
              variable: summary
            - value_selector:
                - code_parse
                - priority
              value_type: string
              variable: priority
            - value_selector:
                - code_parse
                - risk_level
              value_type: string
              variable: risk_level
            - value_selector:
                - code_parse
                - sentiment
              value_type: string
              variable: sentiment
            - value_selector:
                - code_parse
                - language
              value_type: string
              variable: language
            - value_selector:
                - code_parse
                - entities
              value_type: object
              variable: entities
          selected: false
          title: 结束
          type: end
        height: 130
        id: end
        position:
          x: 1100
          y: 260
        positionAbsolute:
          x: 1100
          y: 260
        selected: false
        sourcePosition: right
        targetPosition: left
        type: custom
        width: 244
    viewport:
      x: 0
      y: 0
      zoom: 1.0
  rag_pipeline_variables: []
