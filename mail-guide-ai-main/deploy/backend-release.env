# mail-guide-ai backend release manifest.
# Jenkins / operations should update this file per release, then run:
#   bash mail-guide-ai-main/scripts/linux/selfhosted/apply-backend-release.sh \
#     /data/temp/mail-guide-ai-main /data/service/supabase-selfhost
#
# v1.2.0 清单含 v1.0→v1.2 累计 migration；已执行过的文件脚本会自动跳过。

RELEASE_VERSION="v1.2.0"

# v1.0.x 现网一次性升到 v1.2.0：列出可能未执行的 SQL（按时间顺序）
MIGRATIONS=(
  "20260520120000_automation_12h_twenty_compensation_retries.sql"
  "20260521120000_risk_intercept_retrying_started_at_comment.sql"
  "20260521140000_erp_notify_and_mailbox_signature.sql"
  "20260522140000_user_mailbox_grants_rls.sql"
  "20260522150000_mailbox_scope_closure.sql"
)

# v1.0.1 起调整过 pg_cron；现网若从未跑过 apply-vault-and-cron 则 true（幂等可重复）
RUN_APPLY_VAULT_AND_CRON="true"

APPLY_FUNCTIONS="true"
BACKUP_FUNCTIONS="true"
